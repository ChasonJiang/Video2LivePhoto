# Video2LivePhoto dependencies
pillow>=10.0
pillow-heif>=0.13
piexif>=1.1.3
# Provides an ffmpeg build with libx264/libx265 (needed for genuine HEVC
# Live Photos that work as wallpaper).  A system ffmpeg/ffprobe on PATH is
# also used (ffprobe for inspection).
imageio-ffmpeg>=0.4
